# pubspec.yaml

name: seu_nome_do_projeto
description: Um novo projeto Flutter.

# O resto do seu arquivo, como a versão do SDK

environment:
  sdk: '>=3.0.0 <4.0.0' # Use a versão do SDK que você está usando

dependencies:
  flutter:
    sdk: flutter

  # --- Dependências Essenciais do Projeto ---

  # 1. Gerenciamento de Estado
  provider: ^6.1.1 # Versão estável e atualizada

  # 2. Banco de Dados SQLite
  sqflite: ^2.3.0 # Versão estável e atualizada
  path: ^1.8.3    # Essencial para o sqflite encontrar o caminho do banco de dados

  # 3. Geração de IDs únicos (Usado no Pedido.dart)
  uuid: ^4.2.1    # Versão estável e atualizada

  # O resto das suas dependências, se houver, deve vir aqui.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# O resto do arquivo, como a seção 'flutter' para assets e fontes